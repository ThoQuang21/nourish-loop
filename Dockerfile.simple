# Simple dev-focused Dockerfile for testing
FROM node:22-alpine

WORKDIR /app

# Install bun
RUN npm install -g bun

# Copy package files
COPY package.json bun.lock* ./

# Install deps
RUN bun install

# Copy source
COPY . .

# Expose port
EXPOSE 3000

# Run dev server
CMD ["bun", "run", "dev"]
